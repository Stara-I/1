weight = int(input("weight: "))
unit = input('(L)bs or (K)GS')
if unit.upper() == "L":
    final = weight * 0.45
    print(f"You are {final} kilos")
else:
    final = weight / 0.45
    print(f"You are {final} pounds")
